# Changelog

## Unreleased

- Added a Bun.build-backed React Compiler plugin for Frame Master using `babel-plugin-react-compiler`, with configurable compiler options, documentation, and focused regression tests.
- Added `exclude` pattern support so React Compiler can skip matching files with Bun glob strings or regular expressions.
